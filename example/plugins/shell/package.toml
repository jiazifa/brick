[package]
name = "shell" # 用于声明插件的名称
description = """
    插件的功能描述，也可以是相关使用的教程
    也支持多行显示
"""
version = "0.1.0" # 版本
authors = ["author_name <author@email.com>"]

[shell]
# command = "sh -C" # 如果是执行 shell 脚本，需要添加 -C 选项
# 入口文件
index = "entry_point.sh" # 入口文件